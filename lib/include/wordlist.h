#ifndef LKCHECKER_WORDLIST
#define LKCHECKER_WORDLIST

/*
struct LkEngine {
};

struct LkSuggestion {
};

struct LkSuggestionList {
};

struct LkEngine* lk_intialize();
void lk_speller_free(struct LkEngine* engine);

struct LkSuggestionList* lk_word_suggestion(const char* word, const char* next_word);
struct LkSuggestionList* lk_text_suggestion(const char* text);
struct LkSuggestionList* lk_suggestion_free(struct LkSuggestionList* suggestions);
*/

#endif
